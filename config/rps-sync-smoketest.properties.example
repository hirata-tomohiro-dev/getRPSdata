# Copy config/rps-sync.properties first, then overlay the smoke test settings below.
# The actual Windows smoke test script auto-generates a temporary config from the main config,
# so this file is mainly a reference of the forced values.

sync.maxTables=5
result.dir=result-smoketest
pg.table=rps_table_inventory_smoketest
